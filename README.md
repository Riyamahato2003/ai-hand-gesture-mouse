# AI Hand Gesture Mouse

An AI-based movement recognition tool to control the mouse cursor using hand gestures.

## Features
- Accurate gesture recognition.
- Lightweight and efficient.
- Easy to integrate into existing applications.

## Installation
Follow the instructions in `requirements.txt` to set up the project.

## Usage
Run the `main.py` script to start the application.